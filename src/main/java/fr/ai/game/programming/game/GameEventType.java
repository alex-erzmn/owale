package fr.ai.game.programming.game;

public enum GameEventType {
    UPDATE_BOARD,   // Notify to refresh the board display
    ENABLE_BUTTONS,  // Notify to enable human player's buttons
    GAME_OVER      // Notify that the game is over
}